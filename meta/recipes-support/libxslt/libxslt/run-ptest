#!/bin/sh
# SPDX-License-Identifier: MIT

cd "$(dirname "$0")/tests" || exit 1

output=$(./runtest 2>&1)
ret=$?

echo "$output" | while IFS= read -r line; do
    echo "$line"
done

if [ $ret -eq 0 ]; then
    echo "PASS: runtest"
else
    echo "FAIL: runtest"
fi
